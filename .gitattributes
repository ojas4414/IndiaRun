# Force these as binary so git never applies line-ending conversion
# (autocrlf) to them -- doing so silently corrupts PDF/XLSX internal
# byte offsets and produces an unreadable file.
*.pdf -text binary
*.xlsx -text binary
*.png binary
*.npy binary
*.index binary
